# Grafana admin credentials
GF_ADMIN_USER={{ GF_ADMIN_USER }}
GF_ADMIN_PASSWORD={{ GF_ADMIN_PASSWORD }}

# Grafana server options (optional but explicit)
GF_SERVER_HTTP_ADDR=0.0.0.0
GF_SERVER_HTTP_PORT=3000
